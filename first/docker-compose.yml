services:
  postgres:
    build: .
    container_name: postgres18
    restart: always

    environment:
      POSTGRES_DB: mydb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: qazwsxedc
      POSTGRES_INITDB_ARGS: "--locale=ru_RU.UTF-8 --auth-host=scram-sha-256"

    volumes:
      - postgres_data:/var/lib/postgresql

    ports:
      - "5432:5432"

  adminer:
    image: adminer

    restart: always

    ports:
      - "8080:8080"

volumes:
  postgres_data:

